//create an object that starts the timer
let Timer
//When we click the button two thing will happen
object.addEventListener("click", myScript);
//first when you click the start button you are taken to a second page that loads the form
// At the same time when you click start the timer starts
